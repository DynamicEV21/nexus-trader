# Nexus Trader Python dependencies
# Installed in the lumibot venv: ~/development/trading-bots/lumibot/venv/
# Update this file when adding new top-level dependencies.

# ---------------------------------------------------------------
# Vector memory stack (lancedb + sentence-transformers + Qwen3) is
# INTENTIONALLY NOT listed here. Those packages live in the AQOS venv
# and are invoked from nexus-trade via subprocess bridge
# (src/memory/bridge.py). See AGENTS.md "Vector memory stack" for
# the full architecture and runtime guards.
#
# If you think you need to add lancedb or sentence-transformers here
# because `import lancedb` fails in the lumibot venv — that is BY
# DESIGN. Use the subprocess bridge. Do not pip install.
# ---------------------------------------------------------------

# Already required by lumibot (re-pinned here for standalone nexus-trade use)
pandas>=2.2.0
numpy>=1.20.0
duckdb>=1.0.0
pydantic>=2.0.0

# Optional but useful for analysis
requests>=2.31.0
